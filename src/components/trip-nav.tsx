import Link from "next/link";
export function TripNav({ tripId, active }: { tripId: string; active: "timeline" | "expenses" | "settlement" }) { const items = [{ key: "timeline", label: "خط زمانی" }, { key: "expenses", label: "هزینه‌ها" }, { key: "settlement", label: "تسویه" }] as const; return <nav aria-label="بخش‌های سفر" className="fixed inset-x-0 bottom-0 z-40 mx-auto flex max-w-md justify-around border-t border-slate-200 bg-white/95 p-2 backdrop-blur lg:static lg:mb-6 lg:max-w-none lg:justify-start lg:gap-2 lg:rounded-2xl lg:border"><Link href="/trips" className="hidden min-h-11 items-center rounded-xl px-4 font-bold text-slate-600 lg:flex">سفرها</Link>{items.map((item) => <Link key={item.key} href={`/trips/${tripId}/${item.key}`} className={`flex min-h-11 items-center rounded-xl px-4 text-sm font-bold ${active === item.key ? "bg-emerald-600 text-white" : "text-slate-600"}`}>{item.label}</Link>)}</nav>; }
